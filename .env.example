# TheHydra environment variables
# Copy to .env and replace placeholder values. Never commit secrets.

DJANGO_SETTINGS_MODULE=config.settings.development
DJANGO_SECRET_KEY=change-me-to-a-long-random-value
DJANGO_DEBUG=true
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,web
DJANGO_CSRF_TRUSTED_ORIGINS=http://localhost,http://127.0.0.1,http://localhost:8000,http://localhost:8080,http://localhost:8088
NGINX_PORT=8080

# Local without Docker: set this to true so Django uses SQLite.
USE_SQLITE=true

POSTGRES_DB=thehydra
POSTGRES_USER=thehydra
POSTGRES_PASSWORD=change-me-db-password
POSTGRES_HOST=db
POSTGRES_PORT=5432

DATABASE_URL=postgres://thehydra:change-me-db-password@db:5432/thehydra

# Optional: load demo data on container start (true/false)
LOAD_DEMO_DATA=true
